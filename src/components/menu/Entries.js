import React from 'react';
import { RenderMenu } from './RenderMenu';

function Entries(){
        return(
            <RenderMenu category="Entradas">
            </RenderMenu>
        )
}

export { Entries }